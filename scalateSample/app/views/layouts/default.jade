-@ var title: String = "Default title from template"
-@ var body: String
!!! 5
html(lang="en")
  head
    title #{title}
    -# This works, but does not user @routes
    -# I did not found any way to port the @routes.... in scalate.
    link(rel="stylesheet" type="text/css" href="/assets/css/bootstrap/bootstrap.min.css")

    :javascript
  body
    h1 Template
    #container
      = unescape(body)